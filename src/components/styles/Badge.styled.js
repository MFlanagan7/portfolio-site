import styled from "styled-components"

const StyledBadge = styled.aside`
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    margin: 5px;

    p {
        text-indent: 0;
        font-size: 1.1em;
    }
`

export default StyledBadge